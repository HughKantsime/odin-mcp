{
  "name": "odin-print-farm-mcp",
  "version": "2.0.1",
  "description": "MCP server for 3D print farm operations — live tools for queue, jobs, printers, inventory, alerts, maintenance (v2); plus v1 reference tools for cost calculation, printer recommendations, farm capacity planning. Powered by O.D.I.N.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "odin-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "src/data",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node --test --import tsx test/client.test.ts",
    "prepublishOnly": "npm run build && npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0"
  }
}
